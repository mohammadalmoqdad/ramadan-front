import styled from "styled-components";
import Default from "../shared/styles"
// import login from "../Login/login.styles"

export default styled(Default)`

`;

export const StandardsDropDownList = styled.div`

`;